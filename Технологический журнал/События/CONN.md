Событие **`CONN`** (от слова *Connection* — Соединение) — это твой главный инструмент для работы с сетевыми инженерами и провайдерами. Это «Сетевой коммутатор» твоего кластера 1С.

Самая частая ошибка новичков — путать `CONN` (Соединение) и `SESN` (Сеанс). 
Давай разберем это на пальцах, чтобы ты навсегда запомнил разницу и понял, когда этот лог спасет тебя от обвинений «Ваш сервер 1С обрывает связь!».

---

### 1. В чем разница между `CONN` и `SESN`? (Аналогия с телефоном)

Представь, что бухгалтер звонит тебе на рабочий телефон:
*   **`CONN` (Соединение):** Это **физический провод**, по которому идут гудки. Трубку подняли (`CONN` установился). Трубку положили или кто-то перерезал кабель топором (`CONN` разорвался).
*   **`SESN` (Сеанс):** Это **сам разговор**. Бухгалтер говорит: *"Привет, это Маша, я хочу зайти в базу"* (`SESN` начался). Бухгалтер говорит: *"Всё, я пошла домой, пока"* (`SESN` закончился).

**Почему это важно админу:**
Если у Маши моргнул домашний Wi-Fi или VPN, физический провод оборвется (в логе `CONN` будет ошибка разрыва). Но **разговор (`SESN`) при этом не прерывается!** Сервер 1С будет держать сеанс Маши открытым еще 20 минут, надеясь, что она перезвонит (восстановит `CONN`) и продолжит работу с того же места.

---

### Сценарий А: Соединение к `rmngr`
#### Акт 1: Знакомство
`...CONN, 0, level=INFO, process=rmngr, OSThread=175383, ClientID=72268, Protected=1, Txt='Accepted, client=(2)10.46.162.4:58645, server=(2)10.46.101.22:1641, marker=1'`

*   **`Accepted`**: Сервер (`rmngr`) принял входящее соединение.
*   **`client=...18.42.162.6:58645`**: Это IP-адрес и динамический порт того, кто к нам стучится.
*   **`server=...13.76.101.265:1641`**: Это IP и порт нашего сервера (Менеджера кластера). Заметь, порт **1641** — это твой второй (тестовый) кластер!
*   **`ClientID=72268`**: Сервер выдал этому клиенту внутренний номерок (бейджик), чтобы следить за ним.

#### Акт 2: Взятие на карандаш
`...Txt='Ping direction opened on server: directionID=bcacd371...'`
`...Txt='Connection added to ping direction on server... clientID=72268, address=18.46.132.4:58645'`

*   **Что происходит:** 1С — параноик. Как только кто-то подключается, менеджер кластера сразу говорит: *"Ага, теперь я буду тебя пинговать (проверять пульс), чтобы убедиться, что ты не завис и не порвал кабель"*.
*   Он создает канал для пинга (`direction opened`) и добавляет туда нашего клиента (`added to ping direction`).

#### Акт 3: Внезапный обрыв
`...Descr='server_addr=(2)15.42.161.4:58645 descr=recv returns zero, disconnected line=2282 file=src/rtrsrvc/src/DataExchangeServerImpl...'`

*   **Что произошло:** Это самое важное сообщение в логе. **`recv returns zero`**. Клиент просто взял и положил трубку!
*   Сервер пытался прочитать данные (`recv`), но получил пустоту (`zero`). Он понимает: клиент отключился (`disconnected`).
*   *Примечание:* Это не обязательно авария. Часто клиенты 1С открывают короткие соединения, чтобы спросить одну настройку, и тут же их закрывают.

#### Акт 4: Уборка мусора
`...Txt='Connection removed from ping direction on server... clientID=72268'`
`...Txt='Ping direction closed on server: directionID=bcacd371...'`

*   **Что происходит:** Раз клиент отключился, серверу больше не нужно щупать его пульс. Менеджер кластера убирает его из списка пингования (`removed`) и закрывает канал пинга (`closed`). Сервер экономит ресурсы.

#### Акт 5: Финальный отчет
`...Txt='Incoming connection closed: client disconnected'`
`...t:clientID=72268, t:computerName=ПК_1513, t:applicationName=1CV8C, t:connectID=0, Calls=4`

*   **`client disconnected`**: Официальное подтверждение закрытия связи.
*   **Идентификация (Твой козырь):** Вот тут 1С раскрывает все карты. Мы узнаем, кто именно к нам заходил.
    *   `t:computerName=ПК_1513`: Имя компьютера в локальной сети.
    *   `t:applicationName=1CV8C`: Толстый клиент 1С (Буква `C` на конце означает Client). Если бы это был Тонкий клиент, было бы `1CV8`. Если консоль — `SrvrConsole`.
*   **`Calls=4`**: За эти 0.05 секунды клиент успел сделать к серверу 4 микро-вызова (запроса), получить ответы и отключиться.

---


### Сценарий Б : Соединение к `rphost`

Это — **святая святых безопасности 1С**. На этом скриншоте мы видим пошаговый процесс **Аутентификации пользователя в базе данных (Security / Logon)**.

---

### Разбор блока аутентификации (Построчно):

**Строка 1: Здороваемся с сервером**
`...CONN, 0, level=INFO, process=rphost... Txt='Accepted, client=(2)13.41.162.4:58643, server=(2)15.42.121.22:1660...'`
*   Рабочий процесс `rphost` на порту **1660** (это твой второй, "разработческий" кластер, так как порт из диапазона 1660-1691!) принял соединение от клиента.

**Строка 2: Включаем радар (Надзор)**
`...Txt='Connection added to ping direction...'`
*   Как и `rmngr`, рабочий процесс `rphost` ставит клиента на "прослушку" (пинг), чтобы убить сеанс, если у того пропадет интернет.

**Строки 3-7: Магия безопасности (Фаза "Представься")**
Вот это самое интересное! Здесь начинается танец протокола безопасности (APS — Authentication Protocol Sequence).

1.  **`Txt=eAPSEno` (No Authentication yet)**
    *   Клиент говорит: *"Я пришел, но я еще не авторизован"*.
2.  **`Txt=Srvr: DstUserName1: d.e.eliseev@TEST.LAN (TEST.LAN\d.e.eliseev) (windows client)`**
    *   **Вот он, твой доменный билет!** Клиент (Windows) передал серверу 1С (Linux) токен Kerberos или NTLM.
    *   Сервер `rphost` расшифровал этот токен и понял: *"Ага, ко мне стучится пользователь `d.e.eliseev` из домена `TEST.LAN`. У него операционная система Windows"*.
    *   *Заметь:* Он еще не в базе 1С! Это пока только авторизация на уровне операционной системы.
3.  **`Txt=eAPSEContinue` (Продолжаем разговор)**
    *   Сервер говорит: *"Токен вижу, он валидный. Продолжаем процесс входа"*.
4.  **`Txt=eAPSEFinish` (Успешно)**
    *   Процедура проверки (рукопожатие) закончена.
5.  **`t:applicationName=1CV8C, Txt=Srvr: DstUserName2: d.e.eliseev@TEST.LAN...`**
    *   Сервер финально фиксирует: *"В базу зашел Толстый клиент (`1CV8C`) под доменной учеткой `d.e.eliseev`"*.

---

### Почему таких блоков три подряд? (За секунду)

Ты видишь три одинаковых куска лога: в `01:01.25`, в `01:01.62` и в `01:01.99`. Пользователь (Денис Елисеев) как бы "залогинился" три раза за одну секунду.

**Почему так происходит:**
Это нормальное поведение «Толстого клиента» (`1CV8C`) при работе с базами. 
1. Сначала он подключается, чтобы просто проверить, жива ли база и какая у нее версия. (Первое соединение, авторизовался, отключился).
2. Потом он подключается, чтобы скачать начальные данные (кэш, конфигурацию). (Второе соединение).
3. Третье соединение — это уже установление стабильной связи для реальной работы (открытие интерфейса).

Если бы это был «Тонкий клиент» (`1CV8`), процесс входа выглядел бы немного компактнее.

---

### Шпаргалка: Что значат `applicationName` в `rmngr`

Когда процессы 1С стучатся друг к другу, они представляются внутренними именами. Вот твой словарь админа:

*   **`ServerProcess`** = Это рабочий процесс **`rphost`**.
*   **`AgentProcess`** = Это агент сервера **`ragent`** (Завхоз).
*   **`ManagerProcess`** = Это менеджер кластера **`rmngr`** (Мозг).
*   **`Notification`** = Это внутренний **Сервис уведомлений** (служебный поток, который передает короткие сигналы, например: «блокировка снята», «фоновое задание завершено»).

---

### Звонок с Сервера Лицензий
`client=...145.4213.131.50... computerName=....-lic, applicationName=ServerProcess`

**Кто и куда звонит:**
*   **Кто:** `rphost` (`ServerProcess`), который физически запущен на твоем сервере лицензирования (`1c-erp-lic`, IP `130.66.141.54`).
*   **Куда:** На твой тестовый сервер (`15.46.5431.24`) на порт менеджера кластера **1641**.

**Зачем это соединение:**
Помнишь, ты рассказывал, что **добавил сервер лицензирования как рабочий сервер в три кластера**? 
Вот оно, документальное подтверждение! Рабочий процесс (`rphost`), который запустился на сервере лицензирования, "звонит" своему начальнику (`rmngr` на порту 1641) и говорит: *"Шеф, я жив, я нахожусь на сервере 1c-erp-lic, готов принимать задачи и раздавать лицензии"*. 
Соединение висело с `02:56` до `05:00` (два часа), за это время `rphost` сделал 17 служебных запросов (`Calls=17`) к менеджеру и потом штатно отключился (`recv returns zero`).

---

### Внутренний пульс (Notification)
`client=...3.44.101.23... computerName=....-test.nmz.lan, applicationName=Notification`

**Кто и куда звонит:**
*   **Кто:** Сервис `Notification` с этого же самого сервера (`....-test`, IP `143.45.131.62`).
*   **Куда:** Самому себе (на порт 1641). Обрати внимание, IP клиента и сервера совпадают!

**Зачем это соединение:**
Внутри одного компьютера процессы 1С тоже общаются по сети (через локалхост/внутренний IP). Это работает **Шина уведомлений**. Например, `rphost` закончил фоновое задание и через `Notification` пнул `rmngr`: *"Эй, запиши в базу, что я всё сделал"*. 
За 2.5 часа было сделано 211 таких уведомлений (`Calls=211`).

---

### Общение начальников
В самом низу мы видим еще два интересных коротких лога:

**1. Звонок от Агента:**
`computerName=....-test, applicationName=AgentProcess, Calls=889`
*   Это твой локальный `ragent` звонит своему локальному `rmngr`. Завхоз спрашивает у Мозга: *"У тебя всё нормально? ОЗУ не переполнено? Новые rphost создавать не надо?"*. Сделал аж 889 запросов.

**2. Звонок от другого Менеджера:**
`computerName=....-lic, applicationName=ManagerProcess, Calls=4`
*   Это `rmngr`, который работает на сервере лицензирования, связывается с `rmngr` на твоем тестовом сервере. 
*   **Зачем:** Они синхронизируют данные! Например, тестовый кластер просит: *"Выдай мне программную лицензию для моего пользователя"*.

---
### В `rphost`:

**Accepted**
`...process=rphost... Txt='Accepted, client=...34.1.101.42:49066, server=...34.1.101.42:1660'`
*   **Что происходит:** Рабочий процесс (`rphost`) на порту **1660** принял входящее соединение с того же самого IP (локально, порт 49066). Клиенту присвоен `ClientID=35343`.

**JobScheduler**
`...process=rphost... t:clientID=35343... t:computerName=....-test, t:applicationName=JobScheduler, Calls=4`
*   **Что происходит:** Спустя **2 минуты** это соединение закрывается.
*   **Кто это был:** `applicationName=JobScheduler`. Это **Планировщик заданий**.
*   **Зачем он приходил:** Планировщик позвонил в `rphost` и сказал: *"Эй, рабочий, вот тебе пачка фоновых заданий (например, обмен с сайтом или пересчет итогов), иди выполняй"*.

**BackgroundJob**
*(Это событие произошло **внутри** тех двух минут, пока Планировщик висел на линии).*
`...process=rphost... t:clientID=35345... t:applicationName=BackgroundJob, Calls=11134`
*   **Что это:** Это само **Фоновое задание** (`BackgroundJob`). Оно отработало (или отвалилось по таймауту).
*   **Обрати внимание на `Calls=11134`:** За время своей работы это задание сделало **11 134 вызова** к ядру 1С (это могли быть запросы к базе, проверки условий и т.д.).
*   **Что это дает админу:** Если пользователи жалуются, что в 14:20 сервер дико тормозил, ты смотришь сюда и понимаешь: *"Ага, в это время пахало фоновое задание, которое сделало 11 тысяч вызовов"*.

---

### Отслеживание разрыва соеднинений 

**Строка 4 (16:03.075...):**
`...process=rphost... Txt='Ping direction statistics: address=10.46.101.22:1641, pingTimeout=5000, pingPeriod=1000, period=10160, packetsSent=10, avgResponseTime=0, maxResponseTime=0, packetsTimedOut=0, packetsLost=1, packetsLostAndFound=1'`

**Что это такое:**
Это **отчет о проверке пульса (Ping Statistics)**. Рабочий процесс (`rphost`) каждые несколько секунд «пингует» своего начальника — Менеджера кластера (`rmngr`) на порту **1641**, чтобы убедиться, что кластер жив. И здесь он выводит статистику за последние 10 секунд (`period=10160` мс).

**Расшифровка диагноза (Читай внимательно):**
*   `pingTimeout=5000`: Я жду ответа 5 секунд, прежде чем решить, что всё плохо.
*   `pingPeriod=1000`: Я отправляю пинг каждую секунду.
*   `packetsSent=10`: Я отправил 10 пакетов.
*   `avgResponseTime=0, maxResponseTime=0`: Начальник отвечал мгновенно (меньше 1 мс).
*   **`packetsLost=1`:** **ОДИН ПАКЕТ БЫЛ ПОТЕРЯН!**
    *   *Что это значит:* Внутри твоего сервера (раз IP локальный) на долю секунды произошел «затор». Менеджер кластера не смог ответить на пинг. Скорее всего, процессор был загружен на 100%, или операционная система (Linux) "заморозила" `rmngr` из-за нехватки ОЗУ (своппинг), или сеть "моргнула".
*   **`packetsLostAndFound=1`:** **Пакет нашелся!**
    *   *Что это значит:* Потерянный пакет всё-таки дошел (или дошел следующий). Связь восстановилась до того, как сработал таймаут (5 секунд).
      
---
