


Событие **`CONN`** (от слова *Connection* — Соединение) — это твой главный инструмент для работы с сетевыми инженерами и провайдерами. Это «Сетевой коммутатор» твоего кластера 1С.

Самая частая ошибка новичков — путать `CONN` (Соединение) и `SESN` (Сеанс). 
Давай разберем это на пальцах, чтобы ты навсегда запомнил разницу и понял, когда этот лог спасет тебя от обвинений «Ваш сервер 1С обрывает связь!».

---

### 1. В чем разница между `CONN` и `SESN`? (Аналогия с телефоном)

Представь, что бухгалтер звонит тебе на рабочий телефон:
*   **`CONN` (Соединение):** Это **физический провод**, по которому идут гудки. Трубку подняли (`CONN` установился). Трубку положили или кто-то перерезал кабель топором (`CONN` разорвался).
*   **`SESN` (Сеанс):** Это **сам разговор**. Бухгалтер говорит: *"Привет, это Маша, я хочу зайти в базу"* (`SESN` начался). Бухгалтер говорит: *"Всё, я пошла домой, пока"* (`SESN` закончился).

**Почему это важно админу:**
Если у Маши моргнул домашний Wi-Fi или VPN, физический провод оборвется (в логе `CONN` будет ошибка разрыва). Но **разговор (`SESN`) при этом не прерывается!** Сервер 1С будет держать сеанс Маши открытым еще 20 минут, надеясь, что она перезвонит (восстановит `CONN`) и продолжит работу с того же места.

---

### 2. Главные свойства (колонки) события `CONN`

Если ты откроешь этот лог, ты увидишь техническую инфу о TCP/IP пакетах:

*   **`Txt` (Текст)** — **Самое важное свойство.** Здесь 1С пишет, что произошло с соединением. 
    *   *Норма:* `Connection established` (Соединение установлено).
    *   *Авария:* `Connection reset by peer` (Удаленный узел разорвал соединение) или `Timeout expired` (Таймаут).
*   **`t:computerName` / `t:clientComputerName`** — IP-адреса, между которыми натянут этот «провод».
*   **`AppID` (Приложение)** — Кто инициировал связь (например, `1CV8` — Тонкий клиент, или `Server` — внутренние процессы).

---

### 3. Реальные боевые сценарии (Когда включать `CONN`?)

Этот лог **нельзя держать включенным постоянно**! 1С генерирует тысячи `CONN` в минуту (процессы внутри Linux постоянно открывают и закрывают TCP-порты для общения друг с другом). Мы включаем его точечно, как скальпель.

#### Сценарий А: «Меня постоянно выкидывает из 1С!»
*   **Ситуация:** Пользователи из удаленного офиса (или на удаленке) жалуются, что 1С периодически закрывается с ошибкой *"Сеанс отсутствует или удален"* или *"Ошибка сетевого доступа к серверу"*. Сетевики клянутся, что VPN работает идеально.
*   **Твое расследование:** Ты включаешь лог `CONN`.
*   **Разгадка:** Ты находишь в логе IP-адрес этого пользователя и видишь в свойстве `Txt` ошибки ОС Linux: `10054 (ECONNRESET)` или `10053 (ECONNABORTED)`. 
*   **Твой козырь:** Ты отправляешь этот лог сетевикам и говоришь: *"Сервер 1С ждал данные, но ваш роутер/VPN-шлюз принудительно прислал TCP-пакет с флагом RST (Reset), разорвав физическое соединение. Настраивайте Keep-Alive на маршрутизаторах или чините потери пакетов"*. Шах и мат.

#### Сценарий Б: Кластер сходит с ума (Ошибки портов)
*   **Ситуация:** Ты настроил второй кластер (как мы делали раньше на порту 1640), но он почему-то не запускается, процессы «прыгают».
*   **Твое расследование:** Ты включаешь `CONN` на самом Linux-сервере.
*   **Разгадка:** В свойстве `Txt` ты видишь: `10061 (ETIMEDOUT)` при попытке `rmngr` достучаться до `rphost` на порт `1660`.
*   **Твой козырь:** Ты сразу понимаешь: *"Ага! Я забыл открыть диапазон портов 1660-1691 в `iptables` (фаерволе Linux)!"*. Процессы просто не могут «дозвониться» друг другу внутри самого сервера.

---

Отличный скриншот! Это событие **`CONN`** (Connection) из папки менеджера кластера (`rmngr`).

Здесь перед нами разворачивается **полный жизненный цикл одного сетевого соединения**: от момента, когда клиент поздоровался с сервером, до момента, когда он отключился. Все эти 7 строчек произошли менее чем за **0.05 секунды** (с `01:01.575` до `01:01.623`).

Давай разберем эту «сетевую пьесу» построчно. Это классический паттерн работы 1С.

---

### Акт 1: Знакомство (Строка 1)
`...CONN, 0, level=INFO, process=rmngr, OSThread=175383, ClientID=72268, Protected=1, Txt='Accepted, client=(2)10.46.162.4:58645, server=(2)10.46.101.22:1641, marker=1'`

*   **`Accepted`**: Сервер (`rmngr`) принял входящее соединение.
*   **`client=...10.46.162.4:58645`**: Это IP-адрес и динамический порт того, кто к нам стучится.
*   **`server=...10.46.101.22:1641`**: Это IP и порт нашего сервера (Менеджера кластера). Заметь, порт **1641** — это твой второй (тестовый) кластер!
*   **`ClientID=72268`**: Сервер выдал этому клиенту внутренний номерок (бейджик), чтобы следить за ним.

### Акт 2: Взятие на карандаш (Строки 2 и 3)
`...Txt='Ping direction opened on server: directionID=bcacd371...'`
`...Txt='Connection added to ping direction on server... clientID=72268, address=10.46.162.4:58645'`

*   **Что происходит:** 1С — параноик. Как только кто-то подключается, менеджер кластера сразу говорит: *"Ага, теперь я буду тебя пинговать (проверять пульс), чтобы убедиться, что ты не завис и не порвал кабель"*.
*   Он создает канал для пинга (`direction opened`) и добавляет туда нашего клиента (`added to ping direction`).

### Акт 3: Внезапный обрыв (Строка 4) 🚨
`...Descr='server_addr=(2)10.46.162.4:58645 descr=recv returns zero, disconnected line=2282 file=src/rtrsrvc/src/DataExchangeServerImpl...'`

*   **Что произошло:** Это самое важное сообщение в логе. **`recv returns zero`** на языке сетевых программистов (C++) означает, что сокет (канал связи) на той стороне был закрыт. Клиент просто взял и положил трубку!
*   Сервер пытался прочитать данные (`recv`), но получил пустоту (`zero`). Он понимает: клиент отключился (`disconnected`).
*   *Примечание:* Это не обязательно авария. Часто клиенты 1С открывают короткие соединения, чтобы спросить одну настройку, и тут же их закрывают.

### Акт 4: Уборка мусора (Строки 5 и 6)
`...Txt='Connection removed from ping direction on server... clientID=72268'`
`...Txt='Ping direction closed on server: directionID=bcacd371...'`

*   **Что происходит:** Раз клиент отключился, серверу больше не нужно щупать его пульс. Менеджер кластера убирает его из списка пингования (`removed`) и закрывает канал пинга (`closed`). Сервер экономит ресурсы.

### Акт 5: Финальный отчет (Строка 7)
`...Txt='Incoming connection closed: client disconnected'`
`...t:clientID=72268, t:computerName=NZSLP-PC-1513, t:applicationName=1CV8C, t:connectID=0, Calls=4`

*   **`client disconnected`**: Официальное подтверждение закрытия связи.
*   **Идентификация (Твой козырь):** Вот тут 1С раскрывает все карты. Мы узнаем, кто именно к нам заходил.
    *   `t:computerName=NZSLP-PC-1513`: Имя компьютера в локальной сети.
    *   `t:applicationName=1CV8C`: Толстый клиент 1С (Буква `C` на конце означает Client). Если бы это был Тонкий клиент, было бы `1CV8`. Если консоль — `SrvrConsole`.
*   **`Calls=4`**: За эти 0.05 секунды клиент успел сделать к серверу 4 микро-вызова (запроса), получить ответы и отключиться.

---

### Резюме для тебя как Админа:

Это **идеальный пример штатной работы сетевого протокола 1С**.
1. Клиент (Толстый клиент с ПК `NZSLP-PC-1513`) подключился к менеджеру кластера на порт 1641.
2. Сделал 4 быстрых вызова.
3. Корректно закрыл сокет (`recv returns zero`).
4. Сервер аккуратно почистил за ним очереди пингов и записал финальный лог.

**Зачем тебе нужен этот лог в реальной жизни?**
Если пользователи жалуются: *"Меня выкидывает из базы с ошибкой сети!"*, ты идешь в этот лог `CONN`.
*   Если ты видишь `recv returns zero` — значит, соединение разорвал **сам клиент** (например, закрыли программу через Диспетчер задач или антивирус заблокировал сеть на компе юзера). Сервер тут не виноват.
*   Если ты увидишь там `10054 Connection reset by peer` или `Timeout` — значит, соединение "убило" сетевое оборудование по пути (глючный роутер, отвалившийся VPN).

### Как ответить на собеседовании:
> «Анализ события **CONN** позволяет мне детально профилировать сетевой уровень кластера. На данном фрагменте лога виден полный жизненный цикл TCP-соединения: от фазы `Accepted` и постановки клиента на контроль доступности (Ping direction), до штатного разрыва сессии клиентом (`recv returns zero`). 
> 
> Особую ценность для меня представляет финальная запись, где платформа выгружает атрибуты **`t:computerName`** и **`t:applicationName`**. Это позволяет мне деанонимизировать трафик (в данном случае это Толстый клиент `1CV8C` с рабочей станции `NZSLP-PC-1513`) и доказать сетевым инженерам, на чьей стороне произошел разрыв соединения».

Отличный лог! Если в прошлый раз мы смотрели лог из папки Менеджера (`rmngr`), где клиент (консоль) только здоровался и сразу уходил, то здесь перед нами лог из папки **Рабочего процесса (`rphost`)**.

Это — **святая святых безопасности 1С**. На этом скриншоте мы видим пошаговый процесс **Аутентификации пользователя в базе данных (Security / Logon)**.

На скрине видны три абсолютно одинаковых блока (три попытки входа за 1 секунду). Давай разберем один из них по строчкам, так как это важнейший механизм, если у тебя используется вход по доменным учеткам Windows (SSO/Kerberos).

---

### Разбор блока аутентификации (Построчно):

**Строка 1: Здороваемся с сервером**
`...CONN, 0, level=INFO, process=rphost... Txt='Accepted, client=(2)10.46.162.4:58643, server=(2)10.46.101.22:1660...'`
*   Рабочий процесс `rphost` на порту **1660** (это твой второй, "разработческий" кластер, так как порт из диапазона 1660-1691!) принял соединение от клиента.

**Строка 2: Включаем радар (Надзор)**
`...Txt='Connection added to ping direction...'`
*   Как и `rmngr`, рабочий процесс `rphost` ставит клиента на "прослушку" (пинг), чтобы убить сеанс, если у того пропадет интернет.

**Строки 3-7: Магия безопасности (Фаза "Представься")**
Вот это самое интересное! Здесь начинается танец протокола безопасности (APS — Authentication Protocol Sequence).

1.  **`Txt=eAPSEno` (No Authentication yet)**
    *   Клиент говорит: *"Я пришел, но я еще не авторизован"*.
2.  **`Txt=Srvr: DstUserName1: d.e.eliseev@TEST.LAN (TEST.LAN\d.e.eliseev) (windows client)`**
    *   **Вот он, твой доменный билет!** Клиент (Windows) передал серверу 1С (Linux) токен Kerberos или NTLM.
    *   Сервер `rphost` расшифровал этот токен и понял: *"Ага, ко мне стучится пользователь `d.e.eliseev` из домена `TEST.LAN`. У него операционная система Windows"*.
    *   *Заметь:* Он еще не в базе 1С! Это пока только авторизация на уровне операционной системы.
3.  **`Txt=eAPSEContinue` (Продолжаем разговор)**
    *   Сервер говорит: *"Токен вижу, он валидный. Продолжаем процесс входа"*.
4.  **`Txt=eAPSEFinish` (Успешно)**
    *   Процедура проверки (рукопожатие) закончена.
5.  **`t:applicationName=1CV8C, Txt=Srvr: DstUserName2: d.e.eliseev@TEST.LAN...`**
    *   Сервер финально фиксирует: *"В базу зашел Толстый клиент (`1CV8C`) под доменной учеткой `d.e.eliseev`"*.

---

### Почему таких блоков три подряд? (За секунду)

Ты видишь три одинаковых куска лога: в `01:01.25`, в `01:01.62` и в `01:01.99`. Пользователь (Денис Елисеев) как бы "залогинился" три раза за одну секунду.

**Почему так происходит:**
Это нормальное поведение «Толстого клиента» (`1CV8C`) при работе с базами. 
1. Сначала он подключается, чтобы просто проверить, жива ли база и какая у нее версия. (Первое соединение, авторизовался, отключился).
2. Потом он подключается, чтобы скачать начальные данные (кэш, конфигурацию). (Второе соединение).
3. Третье соединение — это уже установление стабильной связи для реальной работы (открытие интерфейса).

Если бы это был «Тонкий клиент» (`1CV8`), процесс входа выглядел бы немного компактнее.

---

### Зачем тебе этот лог как Админу? (Твой козырь)

Этот лог — **единственный способ доказать, что проблема не в 1С, а в Active Directory.**

**Сценарий из жизни:**
Приходит пользователь и говорит: *"Я открываю 1С, выбираю базу, а она просит логин и пароль в желтом окошке! Я же в домене, она должна входить сама (Single Sign-On)!"*.

Ты открываешь этот лог `CONN` на процессе `rphost`.

*   **Вариант А:** Ты видишь строчку `DstUserName1: ivanov@TEST.LAN`. Значит, Linux и 1С всё поняли. Проблема в том, что в самой 1С (в Конфигураторе) карточка пользователя `Иванов` не привязана к этой доменной учетке (галочка "Аутентификация ОС").
*   **Вариант Б (Чаще всего):** Ты НЕ видишь строчек `DstUserName1` и `eAPSEContinue`, а видишь ошибку авторизации. Это значит, что твой Linux-сервер 1С **не смог прочитать Kerberos-билет** (например, рассинхронизировалось время с контроллером домена, или сломался keytab-файл на Линуксе). Ты с фактами идешь к админам AD и говоришь: *"Почините Kerberos на сервере, токены не проходят!"*.

### Как блеснуть на собеседовании (Про безопасность):
> «Анализ событий **`CONN`** на рабочих процессах (`rphost`) критически важен при диагностике проблем со сквозной аутентификацией (SSO / Active Directory) в смешанных средах Linux-Windows. 
> 
> По фазам протокола APS (Authentication Protocol Sequence) — от **`eAPSEno`** до **`eAPSEFinish`** — я могу отследить, успешно ли сервер приложений расшифровал Kerberos/NTLM токен клиента (поле **`DstUserName1`**). Это позволяет мне четко разделить ответственность: если токен в логе есть, то проблема кроется в маппинге прав внутри самой информационной базы 1С; если токена нет или фаза завершается с ошибкой — проблема на уровне системных библиотек аутентификации (keytab) операционной системы Linux или рассинхронизации времени с контроллером домена».





Этот скриншот — просто жемчужина! Если в прошлый раз мы видели, как к серверу подключается живой пользователь (`1CV8C` — Толстый клиент), то здесь мы видим **«внутреннюю нервную систему» кластера 1С**.

Здесь нет ни одного живого человека. Это логи того, как **серверы и процессы 1С общаются между собой**. 

Так как лог из папки **`rmngr`** (Менеджер кластера), мы видим, кто «звонит» Главному Мозгу твоего кластера (на порт 1641).

Давай расшифруем эти `applicationName` (это системные имена процессов 1С) и разберем, кто кому звонит и зачем.

---

### Шпаргалка: Что значат `applicationName`

Когда процессы 1С стучатся друг к другу, они представляются внутренними именами. Вот твой словарь админа:

*   **`ServerProcess`** = Это рабочий процесс **`rphost`**.
*   **`AgentProcess`** = Это агент сервера **`ragent`** (Завхоз).
*   **`ManagerProcess`** = Это менеджер кластера **`rmngr`** (Мозг).
*   **`Notification`** = Это внутренний **Сервис уведомлений** (служебный поток, который передает короткие сигналы, например: «блокировка снята», «фоновое задание завершено»).

А теперь давай разберем сами подключения (сценарии из твоего лога).

---

### БЛОК 1 (Верхний): Звонок с Сервера Лицензий
`client=...10.46.101.30... computerName=1c-erp-lic, applicationName=ServerProcess`

**Кто и куда звонит:**
*   **Кто:** `rphost` (`ServerProcess`), который физически запущен на твоем сервере лицензирования (`1c-erp-lic`, IP `10.46.101.30`).
*   **Куда:** На твой тестовый сервер (`10.46.101.22`) на порт менеджера кластера **1641**.

**Зачем это соединение:**
Помнишь, ты рассказывал, что **добавил сервер лицензирования как рабочий сервер в три кластера**? 
Вот оно, документальное подтверждение! Рабочий процесс (`rphost`), который запустился на сервере лицензирования, "звонит" своему начальнику (`rmngr` на порту 1641) и говорит: *"Шеф, я жив, я нахожусь на сервере 1c-erp-lic, готов принимать задачи и раздавать лицензии"*. 
Соединение висело с `02:56` до `05:00` (два часа), за это время `rphost` сделал 17 служебных запросов (`Calls=17`) к менеджеру и потом штатно отключился (`recv returns zero`).

---

### БЛОК 2 (Средний): Внутренний пульс (Notification)
`client=...10.46.101.22... computerName=1c-erp-test.nmz.lan, applicationName=Notification`

**Кто и куда звонит:**
*   **Кто:** Сервис `Notification` с этого же самого сервера (`1c-erp-test`, IP `10.46.101.22`).
*   **Куда:** Самому себе (на порт 1641). Обрати внимание, IP клиента и сервера совпадают!

**Зачем это соединение:**
Внутри одного компьютера процессы 1С тоже общаются по сети (через локалхост/внутренний IP). Это работает **Шина уведомлений**. Например, `rphost` закончил фоновое задание и через `Notification` пнул `rmngr`: *"Эй, запиши в базу, что я всё сделал"*. 
За 2.5 часа было сделано 211 таких уведомлений (`Calls=211`).

---

### БЛОК 3 (Нижний): Общение начальников
В самом низу мы видим еще два интересных коротких лога:

**1. Звонок от Агента:**
`computerName=1c-erp-test.nmz.lan, applicationName=AgentProcess, Calls=889`
*   Это твой локальный `ragent` звонит своему локальному `rmngr`. Завхоз спрашивает у Мозга: *"У тебя всё нормально? ОЗУ не переполнено? Новые rphost создавать не надо?"*. Сделал аж 889 запросов.

**2. Звонок от другого Менеджера:**
`computerName=1c-erp-lic, applicationName=ManagerProcess, Calls=4`
*   Это `rmngr`, который работает на сервере лицензирования, связывается с `rmngr` на твоем тестовом сервере. 
*   **Зачем:** Они синхронизируют данные! Например, тестовый кластер просит: *"Выдай мне программную лицензию для моего пользователя"*.

---

### Резюме для тебя как Админа

Этот кусок лога — доказательство того, что **твой кластер работает идеально**. 

Все распределенные узлы (твои серверы `1c-erp-test` и `1c-erp-lic`) видят друг друга, успешно открывают TCP-сессии, обмениваются служебной информацией и корректно закрывают соединения без ошибок обрыва (нет никаких таймаутов или `Connection reset by peer`).

**Что сказать на собеседовании (Высший пилотаж):**
> «При профилировании событий **CONN** в папке менеджера кластера (`rmngr`) я анализирую не только клиентский трафик, но и межузловое взаимодействие (Inter-node communication). 
> 
> По свойству **`t:applicationName`** я могу четко классифицировать системный трафик: `ServerProcess` указывает на обращения рабочих процессов (`rphost`) к менеджеру кластера, `AgentProcess` — на сигналы управления от `ragent`, а `ManagerProcess` фиксирует синхронизацию между менеджерами разных хостов (например, при запросе лицензий с выделенного сервера). 
> Мониторинг этих соединений позволяет мне убедиться в отсутствии сетевой сегментации (split-brain) в распределенном кластере и подтвердить штатную работу микросервисов 1С».
